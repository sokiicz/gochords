# gochords

> Status: wip — created 2026-05-06

## Setup
```bash
pnpm install
pnpm dev
```

## Env vars
See `D:/ai/Apps/Secrets/gochords.txt` (local only, not committed).

## Deploy
GitHub Pages — set `base: '/gochords/'` in `vite.config.ts`, push, enable Pages on `gh-pages` branch.
